Activity Hub {{ .Chart.AppVersion }} is installed as {{ .Release.Name }}.

{{- if .Values.ingress.enabled }}
Open http://{{ .Values.ingress.host }}
{{- else }}
No ingress is enabled. To look at it:

  kubectl --namespace {{ .Release.Namespace }} port-forward svc/{{ include "activity-hub.fullname" . }}-web {{ .Values.web.service.port }}
  open http://localhost:{{ .Values.web.service.port }}
{{- end }}

The dashboard will ask for your name on first run; that is the whole of setup.

{{ if .Values.postgresql.enabled -}}
PostgreSQL is running inside this release as a single replica
{{- if .Values.postgresql.persistence.enabled }} with a {{ .Values.postgresql.persistence.size }} volume.
Nothing here backs it up — that part is yours.
{{- else }} with an emptyDir, so the data is gone when the pod is.
Set postgresql.persistence.enabled=true before this holds anything you want to keep.
{{- end }}
{{- else -}}
Using the database URL from secret {{ .Values.externalDatabase.existingSecret }}.
{{- end }}
