# Where the API lives. Left empty the app calls /api on its own origin, which
# is what both the Vite dev proxy and the nginx image do — no CORS involved.
VITE_API_BASE_URL=

# Only for `npm run dev`: where the dev server proxies /api to.
VITE_API_TARGET=http://localhost:8000
